version=1.0.0-SNAPSHOT
group=pe.edu.nova

# Quarkus platform coordinates (must match the extension's expectation).
# Pinned to the last stable patch of the Nova-canonical LTS line 3.33.
quarkusPlatformGroupId=io.quarkus.platform
quarkusPlatformArtifactId=quarkus-bom
quarkusPlatformVersion=3.33.2.1
quarkusPluginId=io.quarkus
quarkusPluginVersion=3.33.2.1

org.gradle.caching=true
# Configuration cache disabled for Quarkus 3.x (build-time augmentation bugs,
# see nova-java-api-standard-quarkus-extension/gradle.properties for the
# canonical Nova comment).
org.gradle.configuration-cache=false
